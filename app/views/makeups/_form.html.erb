<%= form_with(model: makeup, local: true) do |form| %>
  <% if makeup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(makeup.errors.count, "error") %> prohibited this makeup from being saved:</h2>

      <ul>
        <% makeup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
   <%= form.file_field :picture %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :colour %>
    <%= form.text_field :colour %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.text_field :rating %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
